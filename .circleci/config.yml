version: 2
jobs:
  steps:
    - checkout
    - setup_remote_docker:
      docker_layer_caching: true
    - run:
        name: Start container and verify it's working
        command: |
          set -x
          docker-compose up -d